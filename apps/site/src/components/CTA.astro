---

---

<section>
  <div class="wrapper">
    <h3>Schedule a Quick Discovery Call</h3>

    <span>
      Dive into how our services can benefit your operations in a 15-minute call
    </span>
  </div>
  <div class="wrapper">
    <h3>Need something special?</h3>

    <span>Contact us and discover our individual solutions.</span>
  </div>
  <!-- <div class="wrapper">
    <h3>Invite a Colleague & Get Rewards</h3>

    <span>Receive up to 15% commission every month.</span>
  </div> -->
</section>

<style>
  section {
    color: white;
    font-family: Inter, sans-serif;
    font-style: normal;

    font-size: var(--step-0);
    font-weight: 400;
    line-height: 120%;

    max-width: 92.5rem;
    margin-inline: auto;
    display: flex;
    flex-wrap: wrap;
    padding: var(--space-l-xl);
    column-gap: var(--space-s-m);
    row-gap: var(--space-s);
  }

  div {
    background: var(--neutral-900);
    border-radius: var(--space-xs);
    border: 1px solid var(--neutral-800);
    display: flex;
    flex-direction: column;
    row-gap: var(--space-3xs-2xs);
    padding: var(--space-xs);
    flex: 1 1 calc(330px - var(--space-l-xl) * 2);
  }

  h3 {
    font-weight: 700;
    line-height: 120%;
  }
</style>
